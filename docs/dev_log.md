# Han-Flow 개발 로그

> 이 문서는 2026-06 prototype 당시의 역사 기록이다. 아래의 편집·직렬화 “완성” 표시는 현재
> V1 제품 계약을 뜻하지 않는다. 현재 상태와 다음 작업은
> [제품 비전과 로드맵](vision_and_roadmap.md), [실행 계획](execution_plan.md)을 기준으로 한다.
> 2026-07 이후의 재현 가능한 최신 개발 일지는 [검증 이력](verification_history.md)에 이어서 기록한다.

## [2026-06-06] 프로젝트 킥오프 및 초기 설계

### 완료된 작업
1.  **프로젝트 구조 설계**: Electron, TypeScript 기반의 모듈형 아키텍처 수립. `core`, `main`, `renderer`로 계층 분리.
2.  **HWPX 파싱 전략 수립**: `unzipper`와 `fast-xml-parser`를 활용한 스트리밍 파이프라인 설계 및 스타일 맵핑 JSON 스키마 정의.
3.  **UX 차별화 전략 정의**: 하이브리드 렌더링, 점진적 로딩, macOS 네이티브 최적화 전략 수립.
4.  **개발 환경 설정 리스트 작성**: 필수 의존성 라이브러리 선정.
5.  **문서화**: `architecture.md`, `parsing_strategy.md`, `ux_strategy.md`, `ai_prompts.md` 생성.

### 결정 사항
-   렌더링 엔진은 복잡한 레이아웃 보존을 위해 HTML/Canvas 하이브리드 방식을 채택함.
-   대용량 문서 처리를 위해 파싱 로직은 별도의 Worker Thread에서 실행하도록 설계함.

### 다음 단계
-   `package.json` 실제 생성 및 기본 Electron 보일러플레이트 코드 작성.
-   `header.xml` 파싱을 위한 상세 TypeScript 인터페이스 정의.
-   단위 테스트 환경(Jest) 구성.


## [2026-06-06] HWPX 파서 핵심 로직 구현

### 완료된 작업
1.  **GitHub 원격 저장소 연결**: `https://github.com/Seongwonp/han-flow.git`에 프로젝트를 연결하고 초기 코드를 푸시했습니다.
2.  **HWPX 파서 핵심 로직 구현**: `src/core/parser/parser.ts` 파일에 `parseHWPX` 함수를 구현하여 HWPX 파일의 ZIP 압축을 해제하고 `header.xml`, `sectionN.xml` 파일들을 `fast-xml-parser`를 이용해 XML에서 JSON으로 변환하는 로직을 추가했습니다.
3.  **HWPX 타입 정의**: `src/shared/types.ts` 파일에 OWPML 표준 스키마를 기반으로 HWPX 문서 구조를 위한 TypeScript 인터페이스들을 정의했습니다.
4.  **스타일 맵핑 로직 추가**: `normalizeDocument` 함수에 `header.xml`에서 폰트, 테두리, 글자 모양, 단락 모양 등 다양한 스타일 정보를 추출하여 ID 기반으로 맵핑하는 로직을 추가했습니다.
5.  **본문 내용 정규화 초기 구현**: `sectionN.xml`에서 단락(`hp:p`), 텍스트 런(`hp:run`), 테이블(`hp:tbl`), 컨트롤(`hp:ctrl`)을 추출하여 `NormalizedDocument` 구조로 변환하는 초기 로직을 구현했습니다.

### 결정 사항
-   `normalizeDocument` 함수 내에서 스타일 정보는 ID를 키로 하는 Map 형태로 관리하여 빠른 참조가 가능하도록 했습니다.
-   본문 내용은 단락을 기준으로 정규화하며, 각 단락 내의 텍스트 런, 테이블, 컨트롤 등은 별도의 타입으로 정의하여 관리합니다.

### 다음 단계
-   스타일 맵핑 및 데이터 정규화 로직을 더욱 견고하게 다듬고, 누락된 필드나 복잡한 구조(예: 테이블 셀 내부의 단락)에 대한 처리를 추가합니다.
-   단위 테스트를 작성하여 파서의 정확성을 검증합니다.


## [2026-06-06] 스타일 맵핑 및 데이터 정규화 모듈 구현

### 완료된 작업
1.  **Normalization 모듈 분리**: `parser.ts`에 있던 `normalizeDocument` 함수를 `src/core/parser/normalization.ts` 파일로 분리하여 모듈성을 강화했습니다.
2.  **Normalization 로직 개선**: `normalizeDocument` 함수 내에서 테이블 셀 내부의 단락 처리 로직을 추가하고, 텍스트 런 내의 탭(`hp:tab`)과 줄바꿈(`hp:lineBreak`) 요소도 정규화된 텍스트로 변환하도록 개선했습니다.
3.  **Parser 모듈 업데이트**: `parser.ts` 파일에서 `normalizeDocument` 함수를 제거하고, `normalization.ts`에서 해당 함수를 import하여 사용하도록 수정했습니다.

### 결정 사항
-   HWPX 파싱 로직과 정규화 로직을 분리하여 코드의 가독성과 유지보수성을 높였습니다.
-   테이블과 같은 복잡한 구조 내의 콘텐츠도 재귀적으로 정규화하여 내부 모델의 일관성을 유지합니다.

### 다음 단계
-   기본 렌더링 엔진 프로토타입을 구현하여 정규화된 문서를 화면에 표시합니다.
-   스타일 적용 로직을 구체화하고, CSS 클래스 정의를 통해 렌더링 품질을 향상시킵니다.


## [2026-06-06] 기본 렌더링 엔진 프로토타입 구현

### 완료된 작업
1.  **렌더링 엔진 프로토타입 구현**: `src/core/renderer-engine/renderer.ts` 파일에 `renderDocument` 함수를 구현하여 정규화된 문서 객체를 HTML 요소로 변환하는 초기 로직을 작성했습니다.
2.  **기본 구조 렌더링**: 문서의 섹션과 단락을 `div` 및 `p` 태그로 렌더링하고, 텍스트 런의 내용을 `span` 태그로 표시하는 기본 기능을 포함했습니다.

### 결정 사항
-   초기 렌더링은 HTML 기반으로 진행하며, 복잡한 레이아웃(테이블, 컨트롤)은 TODO로 남겨두어 추후 Canvas/SVG 기반 렌더링과 통합할 예정입니다.
-   스타일 적용은 각 요소에 클래스를 부여하고 CSS를 통해 관리하는 방식으로 설계했습니다.

### 다음 단계
-   렌더링 엔진에 HWPX 스타일(글자 모양, 단락 모양 등)을 적용하는 로직을 구현합니다.
-   테이블 및 컨트롤과 같은 복잡한 요소의 렌더링을 구체화합니다.
-   개발 로그와 설계 문서를 최종 업데이트하고, 전체 결과물을 사용자에게 전달합니다.


## [2026-06-06] 빌드 오류 해결 및 Electron 보일러플레이트 구축

### 완료된 작업
1.  **빌드 오류 해결**: `electron-vite` 설정 부재로 인한 빌드 오류를 `electron.vite.config.ts` 파일을 생성하여 해결했습니다.
2.  **Electron 보일러플레이트 구축**: Main Process(`src/main/index.ts`), Preload Script(`src/main/preload.ts`), Renderer Process(`src/renderer/index.html`, `src/renderer/src/main.tsx`)의 진입점과 기본 코드를 작성했습니다.
3.  **macOS 최적화 설정**: Main Process에서 `titleBarStyle: 'hiddenInset'` 설정을 통해 macOS 네이티브 앱과 유사한 상단 바 스타일을 적용했습니다.
4.  **빌드 검증**: `npm run build` 명령을 통해 Main, Preload, Renderer 프로세스가 모두 정상적으로 빌드됨을 확인했습니다.

### 결정 사항
-   의존성 문제로 인해 `@electron-toolkit/utils` 사용을 최소화하고, 필요한 기능은 직접 구현하거나 표준 Node.js/Electron API를 사용하도록 수정했습니다.
-   Vite와 React를 사용하여 현대적이고 빠른 렌더러 개발 환경을 구축했습니다.

### 다음 단계
-   구현된 HWPX 파서와 렌더링 엔진을 Electron UI에 통합합니다.
-   실제 HWPX 파일을 불러와 화면에 렌더링하는 기능을 구현합니다.


## [2026-06-06] 경로 설정 오류 해결 및 빌드 최적화

### 완료된 작업
1.  **경로 설정 오류 해결**: `electron-vite`가 빌드 결과물을 찾지 못하는 문제를 해결하기 위해 `electron.vite.config.ts`에서 각 프로세스별 `outDir`을 명시적으로 설정했습니다.
2.  **package.json 최적화**: `main` 경로를 명확히 하고, `build` 설정을 추가하여 출력 디렉토리 구조를 정리했습니다.
3.  **클린 빌드 검증**: 기존 빌드 결과물을 삭제하고 재빌드하여 `out/` 폴더 아래에 `main`, `preload`, `renderer` 결과물이 정상적으로 생성됨을 확인했습니다.

### 결정 사항
-   개발 및 빌드 시의 경로 혼선을 방지하기 위해 모든 출력 경로를 `out/` 디렉토리로 통일하고 명시적으로 관리합니다.

### 다음 단계
-   사용자 환경에서 `npm run dev` 실행 시 정상 작동 여부 확인.
-   HWPX 파일 선택 및 로딩 기능 구현 시작.


## [2026-06-06] 파일 열기 기능 및 엔진 연동

### 완료된 작업
1.  **IPC 통신 채널 구축**: 메인 프로세스와 렌더러 프로세스 간의 파일 열기(`dialog:openFile`) 및 파싱(`hwpx:parse`)을 위한 IPC 핸들러를 구축했습니다.
2.  **React UI 고도화**: `App.tsx`를 업데이트하여 파일 선택 버튼과 드래그 앤 드롭 기능을 추가했습니다.
3.  **엔진 연동**: UI에서 선택된 HWPX 파일을 메인 프로세스의 파서로 전달하고, 파싱된 JSON 데이터를 다시 UI로 받아와 화면에 렌더링하는 전체 파이프라인을 완성했습니다.
4.  **macOS 스타일링**: A4 용지 스타일의 뷰어 영역과 macOS 네이티브 느낌의 헤더를 구현했습니다.

### 결정 사항
-   보안 및 성능을 위해 파일 읽기와 파싱은 메인 프로세스에서 수행하고, 결과인 정규화된 JSON 데이터만 렌더러로 전달합니다.
-   드래그 앤 드롭 지원을 통해 macOS 사용자의 편의성을 높였습니다.

### 다음 단계
-   표(Table) 및 이미지(Image) 렌더링 기능을 구체화합니다.
-   스타일 정보(폰트, 크기, 정렬 등)를 실제 UI에 반영하는 로직을 강화합니다.


## [2026-06-06] 파싱 안정성 강화 및 .hwp 지원 시작

### 완료된 작업
1.  **HWPX 파서 안정화**: XML 네임스페이스(`hp:`, `hh:`, `hs:`)가 없거나 다른 경우에도 유연하게 데이터를 찾을 수 있도록 `getVal` 헬퍼 함수를 도입하고 방어적 로직을 추가했습니다.
2.  **구조 유연화**: `headerData`나 `sectionsData` 내의 특정 필드가 누락되어도 앱이 크래시되지 않도록 Null Check를 강화했습니다.
3.  **.hwp 파일 인식**: 파일 열기 대화상자와 드래그 앤 드롭 UI에서 `.hwp` 확장자를 인식하고 처리할 수 있도록 업데이트했습니다.
4.  **바이너리 파서 구조 마련**: `src/core/parser/hwp_parser.ts`를 생성하여 `.hwp` 바이너리 파싱을 위한 아키텍처를 설계했습니다.

### 결정 사항
-   HWPX는 공식 표준(OWPML)을 따르지만, 실제 생성된 파일들은 네임스페이스 사용이 제각각일 수 있으므로 유연한 키 매칭 방식을 채택했습니다.
-   `.hwp` 지원은 우선 UI와 IPC 수준에서 경로를 확보하고, 실제 바이너리 파싱 엔진은 단계적으로 구현하기로 했습니다.

### 다음 단계
-   사용자로부터 받은 실제 HWPX 파일의 파싱 성공 여부 재확인.
-   `.hwp` 바이너리 파싱을 위한 외부 라이브러리 검토 및 스트림 추출 로직 구현.


## [2026-06-06] 렌더링 최적화 및 HWP 바이너리 파서 연동 시작

### 완료된 작업
1.  **점진적 렌더링(Incremental Rendering) 도입**: 대용량 HWPX 파일 로딩 시 브라우저가 멈추는 현상을 방지하기 위해, 초기 50개 단락만 먼저 그리고 스크롤 시 추가로 로드하는 방식을 적용했습니다.
2.  **파싱 안전장치 추가**: `normalization.ts`에서 단락 파싱 개수를 최대 10,000개로 제한하고 섹션별 로깅을 강화하여 무한 루프 가능성을 차단했습니다.
3.  **HWP 바이너리 라이브러리 연동**: `cfb` 라이브러리를 설치하고 `hwp_parser.ts`에 연동하여 `.hwp` 파일의 내부 스트림(CFBF)을 추출할 수 있는 기초 로직을 구현했습니다.
4.  **UI/UX 개선**: 로딩 중 상태 표시와 대용량 문서 처리를 위한 무한 스크롤 기초 로직을 `App.tsx`에 반영했습니다.

### 결정 사항
-   성능 향상을 위해 `useMemo`를 사용하여 불필요한 리렌더링을 방지했습니다.
-   `.hwp` 지원을 위해 `cfb` 라이브러리를 사용하여 Compound File 구조를 분석하기 시작했습니다.

### 다음 단계
-   `.hwp` 파일의 `BodyText` 스트림에서 zlib 압축을 해제하고 실제 텍스트 레코드를 추출합니다.
-   표(Table) 내의 텍스트 렌더링 버그를 수정하고 스타일 반영을 고도화합니다.


## [2026-06-06] 새 문서 기반 실시간 편집 시스템 구축

### 완료된 작업
1.  **초기 화면 전환**: 앱 실행 시 즉시 '새 문서'가 생성되도록 변경하여 사용자 경험을 한글/워드와 유사하게 개선했습니다.
2.  **Zustand 상태 관리 도입**: 복잡한 문서 구조를 효율적으로 관리하고 실시간 편집 성능을 보장하기 위해 `Zustand`를 도입했습니다.
3.  **실시간 편집(ContentEditable) 구현**: `contentEditable`을 활용하여 사용자가 화면에서 직접 텍스트를 입력하고 수정할 수 있는 기초 기능을 구현했습니다.
4.  **macOS 네이티브 UI 레이아웃**: 상단 툴바, A4 용지 스타일의 에디터, 하단 상태바 등 macOS 최적화된 레이아웃을 설계했습니다.
5.  **엔터 키 처리**: 엔터 키 입력 시 새로운 단락이 생성되는 로직을 상태 관리자에 추가했습니다.

### 결정 사항
-   "편집 우선" 전략에 따라 불러오기 기능보다 편집 안정성과 성능을 우선적으로 확보합니다.
-   데이터 보안을 위해 모든 편집 데이터는 로컬 상태(`Zustand`)에서만 처리되며, 저장 시에만 파일 시스템에 접근합니다.

### 다음 단계
-   글꼴 크기, 굵게, 기울임 등 서식 적용 기능(CharPr)을 툴바와 연동합니다.
-   작성 중인 문서를 `.hwpx` 형식으로 로컬에 저장하는 기능을 구현합니다.


## [2026-06-06] 한글 입력(IME) 충돌 문제 해결

### 완료된 작업
1.  **IME 이벤트 핸들링 도입**: `onCompositionStart`, `onCompositionEnd` 이벤트를 사용하여 한글 조합 중에는 상태 업데이트를 중단하고, 조합이 완료된 시점에만 데이터를 동기화하도록 수정했습니다.
2.  **비제어 방식(Uncontrolled) 최적화**: React의 강제 리렌더링이 브라우저의 IME 조합 과정을 방해하지 않도록, 입력 영역을 비제어 방식으로 운영하여 글자 깨짐 및 중복 입력 현상을 근본적으로 해결했습니다.
3.  **상태 업데이트 최적화**: `Zustand` 스토어에서 텍스트가 실제로 변경된 경우에만 상태를 업데이트하도록 방어적 로직을 추가하여 성능을 높였습니다.

### 결정 사항
-   한글 에디터의 핵심은 IME 안정성이므로, React의 선언적 렌더링보다 브라우저의 네이티브 입력 경험을 우선시하는 아키텍처를 채택합니다.

### 다음 단계
-   글자 서식(굵게, 밑줄 등) 적용 시에도 IME 조합이 깨지지 않도록 정밀한 선택 영역(Selection) 관리 기능을 추가합니다.
-   문서 저장 및 내보내기 기능을 구현합니다.


## [2026-06-06] 단락 삭제 및 백스페이스 동작 안정화

### 완료된 작업
1.  **스마트 백스페이스 구현**: 단락이 비어있을 때 백스페이스를 누르면 해당 단락을 삭제하고 이전 단락으로 포커스를 이동하는 로직을 추가했습니다.
2.  **커서 위치 관리**: 단락 삭제 후 이전 단락의 끝으로 커서가 자동으로 이동하도록 `Selection` API를 사용하여 UX를 개선했습니다.
3.  **데이터 유실 방지**: 모든 단락이 삭제되지 않도록 최소 한 개의 단락은 항상 유지하는 방어 로직을 스토어에 적용했습니다.
4.  **고유 ID 기반 렌더링**: 단락마다 고유한 ID를 생성하고 이를 React의 `key` 값으로 사용하여, 단락 삭제 시 다른 단락들이 예기치 않게 사라지거나 리렌더링되는 문제를 해결했습니다.

### 결정 사항
-   브라우저의 기본 삭제 동작이 React 상태와 충돌하지 않도록 `e.preventDefault()`를 적절히 사용하고 수동으로 상태를 관리합니다.

### 다음 단계
-   글자 서식(굵게, 크기 등) 변경 시에도 단락 구조가 깨지지 않도록 정밀한 렌더링 로직을 구축합니다.
-   문서 자동 저장 기능을 추가합니다.


## [2026-06-06] Ref 기반 비제어 에디터 전환 및 중복 입력 해결

### 완료된 작업
1.  **완전 비제어 방식(Uncontrolled) 전환**: React의 상태 업데이트가 입력 필드를 다시 그리는 현상을 원천 차단하기 위해, 각 단락을 `ParagraphItem` 컴포넌트로 분리하고 `Ref`를 사용하여 직접 DOM을 제어하도록 수정했습니다.
2.  **중복 입력 버그 해결**: 한글 입력 및 스페이스바 입력 시 데이터가 중복으로 쌓이는 문제를 해결하기 위해, 입력 중에는 리렌더링을 방지하고 조합이 끝난 시점이나 특정 키 입력 시점에만 데이터를 동기화하도록 로직을 강화했습니다.
3.  **성능 최적화**: `React.memo`를 사용하여 수정 중인 단락 외의 다른 단락들이 불필요하게 다시 그려지지 않도록 최적화했습니다.

### 결정 사항
-   React의 선언적 UI와 브라우저의 네이티브 입력(IME) 간의 충돌을 피하기 위해, 에디터 코어 부분은 '비제어 컴포넌트' 패턴을 엄격히 적용합니다.

### 다음 단계
-   사용자 피드백을 통해 입력 안정성을 최종 확인합니다.
-   텍스트 서식(Bold, Italic 등) 적용을 위한 Range/Selection 처리 기능을 추가합니다.


## [2026-06-06] 프로페셔널 워드 프로세서로의 진화 (Major Milestone)

### 완료된 작업
1.  **HWP 스타일 리본 메뉴(Ribbon Menu) 구현**: 
    - 한컴오피스 2020의 UI 체계를 이식하여 [파일, 편집, 입력, 검토] 탭 기반의 그루핑 메뉴 시스템을 구축했습니다.
    - `react-icons`를 도입하여 고해상도 벡터 아이콘으로 UI 전문성을 확보했습니다.
2.  **표(Table) 정규화 및 렌더링 엔진 전면 개편**:
    - OWPML 형제 노드 구조를 인식하도록 파서를 수정하여 문서 내 모든 표를 정확한 위치에 렌더링하도록 개선했습니다.
    - 슬래시(/) 메뉴 및 리본 메뉴를 통한 **동적 표 삽입 기능**을 실구현했습니다.
3.  **지능형 서식 감지 및 상태 동기화**:
    - 커서 위치에 따른 굵게, 기울임 등 서식 적용 상태를 리본 메뉴에 실시간으로 반영(Active State)하는 기능을 추가했습니다.
    - 텍스트 색상 및 형광펜 색상을 자유롭게 선택할 수 있는 **컬러 피커(Color Picker)**를 연동했습니다.
4.  **고급 파일 관리 및 워크플로우**:
    - **isDirty** 상태 관리를 통해 저장되지 않은 변경 사항이 있을 경우 '저장 확인' 팝업을 띄우는 안전장치를 마련했습니다.
    - **멀티 윈도우 지원**: 파일을 열 때 '새 창에서 열기' 옵션을 제공하여 다중 문서 작업 환경을 구축했습니다.
5.  **시스템 폰트 스캐닝**: `font-list` 라이브러리를 연동하여 사용자의 Mac에 설치된 모든 폰트를 에디터에서 즉시 사용할 수 있도록 구현했습니다.
6.  **스크롤 및 레이아웃 최종 해결**: CSS Flexbox 구조 개선을 통해 대용량 문서에서도 끊김 없는 **네이티브 스크롤**과 A4 페이지 가이드를 완성했습니다.

### 결정 사항
- 단순한 뷰어나 마크다운 에디터를 넘어, 실제 한글(HWPX)의 복잡한 개체 구조를 완벽히 수용하는 **'상용 수준의 워드프로세서'**로 프로젝트 스케일을 격상했습니다.
- 사용자 편의성을 위해 '입력 즉시 전환'되는 마크다운 단축키와 '전문가용 리본 메뉴'를 결합한 하이브리드 UX를 유지하기로 했습니다.

### 다음 단계
- 표의 셀 병합/분할 및 정밀한 선 스타일 설정 기능 구현.
- 그림(Image) 및 차트(Chart) 개체의 드래그 앤 드롭 삽입 기능.
- HWPX 직렬화 엔진 고도화 (글자 색상, 표 스타일 정보 포함).

## [2026-06-08] Han-Flow Pro: 엔진 고도화 및 데이터 정규화 완성

### 완료된 작업
1.  **정밀 렌더링 엔진 분리**: `core/renderer-engine/renderer.ts`를 통해 HWPX 스타일(`CharPr`, `ParaPr`)을 실제 CSS 속성으로 매핑하는 전문적인 스타일 해설기(Style Resolver)를 구현했습니다.
2.  **전역 스타일 시트 자동 생성**: 문서의 모든 스타일 ID를 기반으로 클래스 기반 CSS를 동적 생성하여 대용량 문서에서도 효율적인 렌더링 성능을 확보했습니다.
3.  **불변성(Immutability) 최적화**: Zustand 스토어를 개편하여 변경된 단락만 정밀하게 업데이트하도록 최적화함으로써 타이핑 지연(Lag)을 제거했습니다.
4.  **역직렬화(Run Splitting) 고도화**: `execCommand` 등으로 복잡해진 DOM 구조를 분석하여 스타일 변화 지점마다 텍스트 런(`Run`)을 분리하여 저장하는 로직을 구현했습니다. 이를 통해 텍스트 중간의 부분 서식도 완벽하게 HWPX로 저장할 수 있습니다.
5.  **HWPX 저장 기능(Serialization) 완성**: 편집된 JSON 데이터를 다시 OWPML 표준 XML로 변환하고 ZIP으로 패키징하여 `.hwpx` 파일로 내보내는 전체 워크플로우를 완성했습니다.
6.  **커서 스타일 실시간 동기화**: 커서의 현재 위치에 해당하는 HWPX 스타일 정보를 역추적하여 리본 메뉴의 버튼 상태와 실시간으로 동기화하는 기능을 추가했습니다.

### 결정 사항
-   상용 수준의 워드프로세서 퀄리티를 위해 데이터 모델의 무결성을 최우선으로 하며, 브라우저의 네이티브 입력 경험과 HWPX 표준 구조 사이의 간극을 `syncDOMToRuns` 로직으로 해결했습니다.

### 다음 단계
-   **표(Table) 편집 고도화**: 셀 병합/분할, 테두리 스타일 지정 및 셀 배경색 편집 기능.
-   **개체(Image) 처리**: 이미지 드래그 앤 드롭 삽입 및 위치 제어 기능.
-   **고급 편집 기능**: 다단 설정, 구역 나누기, 머리말/꼬리말 지원.
-   **내일의 목표**: 이미지 삽입 기능과 정밀한 표 편집 UI 구현 시작.

## [2026-09-04] 표 셀·행 편집 기반 완성과 다음 관문 확정

### 완료된 작업

1. 병합되지 않은 일반 body cell의 여러 문단 선택·치환·분할·병합을 transaction history에 연결했습니다.
2. 공유 style을 직접 바꾸지 않는 셀 테두리·배경 편집과 `table-cell-style` 저장 안내를 구현했습니다.
3. 단순 직사각형 표의 안전한 행 추가·삭제, `rowCnt`·주소·전체 높이 갱신과 exact undo/redo를 구현했습니다.
4. 행 삭제 후 다음 body 행, 마지막 행이면 이전 body 행으로 selection을 재배치하도록 안정화했습니다.
5. typecheck, Jest 36 suites·206 tests, production build와 parser probe 8종을 통과했습니다.

### 결정 사항

- 표 구조 편집은 원본 table fragment를 보존하는 원자적 command와 fail-closed 정책을 유지합니다.
- 열 편집은 모든 행의 anchor ordinal과 표 너비를 바꾸므로 행 편집과 분리해 검증합니다.
- 셀 병합·분할은 열 추가·삭제가 완료된 뒤 별도 topology 관문으로 진행합니다.

### 다음 시작점

1. 여러 열 공개 fixture와 열 topology preflight를 먼저 추가합니다.
2. 오른쪽 열 추가에서 행별 cell 복제, `colCnt`·`colAddr`·표 너비와 selection projection을 구현합니다.
3. 현재 열 삭제에서 다음/이전 cell selection 재배치와 살아남은 inverse locator를 구현합니다.
4. main session, 리본, loss policy, Save As·재개봉과 전체 자동 회귀를 연결합니다.

## [2026-09-05] 안전한 오른쪽 열 추가 기반 구현

### 완료된 작업

1. 다중 열 공개 fixture와 단순 직사각형 topology 검증을 추가했습니다.
2. 모든 direct row와 반복 머리글에서 선택 열 오른쪽에 같은 모양의 빈 cell을 복제했습니다.
3. `colCnt`, 뒤쪽 `colAddr`, 표 전체 너비와 source selection ordinal을 원자적으로 갱신했습니다.
4. exact undo/redo, main IPC, 리본, loss policy와 Save As·재개봉을 연결했습니다.
5. typecheck, Jest 36 suites·210 tests, production build와 parser probe 8종을 통과했습니다.

### 다음 시작점

- 현재 열 삭제와 다음 cell, 마지막 열이면 이전 cell로 selection을 재배치하는 경로를 구현합니다.
- 마지막 하나뿐인 열과 불균일 geometry를 source 변경 없이 차단합니다.
